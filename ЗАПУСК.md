# 🚀 Инструкция по запуску бота

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

Или для Windows PowerShell:
```powershell
python -m pip install -r requirements.txt
```

## Шаг 2: Создание файла .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```
BOT_TOKEN=ваш_токен_бота_здесь
DATABASE_PATH=database.db
ADMIN_IDS=ваш_telegram_id

# Настройки AirbaPay (опционально - можно оставить пустыми)
AIRBA_PAY_BASE_URL=https://ps.airbapay.kz/acquiring-api
AIRBA_PAY_USER=
AIRBA_PAY_PASSWORD=
AIRBA_PAY_TERMINAL_ID=
AIRBA_PAY_COMPANY_ID=230140022645
AIRBA_PAY_WEBHOOK_URL=
```

### Как получить BOT_TOKEN:
1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Скопируйте токен в файл `.env`

### Как узнать свой Telegram ID:
1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Скопируйте ваш ID в `ADMIN_IDS` в файле `.env`

## Шаг 3: Запуск бота

### Вариант 1: Через командную строку

```bash
python main.py
```

### Вариант 2: Через PowerShell (Windows)

```powershell
python main.py
```

### Вариант 3: Использовать готовый скрипт

Запустите файл `ЗАПУСК.bat` (двойной клик)

## ✅ Проверка работы

После запуска вы должны увидеть в консоли:
```
База данных инициализирована
Бот запущен
```

Если видите ошибку о токене:
```
BOT_TOKEN не установлен! Установите его в файле .env
```

Проверьте, что файл `.env` создан и содержит правильный токен.

## 🛑 Остановка бота

Нажмите `Ctrl+C` в терминале, где запущен бот.

## 📝 Примечания

- База данных создается автоматически при первом запуске
- Логи сохраняются в файл `bot.log`
- Если платежная система не настроена, бот работает без оплаты
- QR-коды требуют установки библиотеки Pillow (устанавливается автоматически)

## ❓ Проблемы при запуске

### Ошибка "ModuleNotFoundError"
Установите зависимости:
```bash
pip install -r requirements.txt
```

### Ошибка "BOT_TOKEN не установлен"
Проверьте наличие файла `.env` и правильность токена

### Ошибка при установке Pillow
Попробуйте установить отдельно:
```bash
pip install pillow
```

Или используйте версию без Pillow (QR-коды будут в виде текста)


